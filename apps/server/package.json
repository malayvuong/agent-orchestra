{
  "name": "@agent-orchestra/server",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "echo 'Server build — not yet configured'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-orchestra/core": "workspace:*",
    "@agent-orchestra/shared": "workspace:*"
  }
}
